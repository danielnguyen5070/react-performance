import React from 'react'
//import { getMatchingPosts } from '../../shared/blog-posts'

function App() {
	return (
			<div>
				{/* create a Form with: (add tailwindcss style)*/}
				{/* 
					- a sreach input
					- 3 checkbox inputs (cat, dog, caterpillar)
					- a button to submit the form 
				*/}
				TO-DO
			</div>
	)
}

// create a component MatchingPosts (add some tailwind styles)
// - display a panel using background color from the post
// - accept a query prop
// - use the getMatchingPosts function to get the matching posts
// - have a list of tags to display

export default App